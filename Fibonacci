#include <stdio.h>
int main(){
    int num1=0, num2=1,i,next;
    printf("First 10 Fibonacci numbers: \n");
    for(i=1, i<=10;i++;){
        printf("%d\n", num1);
        next=num1+num2;
        num1=num2;
        num2=next;
    }
    return 0;
}
